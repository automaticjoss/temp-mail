name = "email-bridge"
main = "worker.ts"
compatibility_date = "2024-01-01"

# Environment variables (set these in Cloudflare Dashboard)
# [vars]
# WEBHOOK_URL = "https://your-vercel-app.vercel.app/api/inbound"
# API_KEY = "your-secret-key"

# Email routing configuration
# Configure this in Cloudflare Dashboard under Email > Email Routing
# Add a custom address rule to route emails to this worker
